# RevenueCat API Keys
# Get these from your RevenueCat dashboard: https://app.revenuecat.com
# Create a .env file with your actual keys (don't commit .env!)

# IMPORTANT: Must use EXPO_PUBLIC_ prefix for client-side access in Expo
# These work in:
# - Expo Go (SDK 49+)
# - Development builds
# - EAS builds (when set as env vars or secrets)

# Option 1: Platform-specific keys (Recommended)
EXPO_PUBLIC_REVENUECAT_IOS_API_KEY=appl_YOUR_IOS_API_KEY_HERE
EXPO_PUBLIC_REVENUECAT_ANDROID_API_KEY=goog_YOUR_ANDROID_API_KEY_HERE

# Option 2: Single shared key (if you only have one API key)
EXPO_PUBLIC_REVENUE_CAT_API_KEY=your_shared_api_key_here

# Alternative: You can also set these in app.json extra field:
# "extra": {
#   "revenueCatIosApiKey": "appl_YOUR_IOS_API_KEY_HERE",
#   "revenueCatAndroidApiKey": "goog_YOUR_ANDROID_API_KEY_HERE"
# }

# For EAS builds, you can also set these as EAS secrets:
# eas secret:create --name EXPO_PUBLIC_REVENUECAT_IOS_API_KEY --value "your_key"
# eas secret:create --name EXPO_PUBLIC_REVENUECAT_ANDROID_API_KEY --value "your_key"
# OR use a single key:
# eas secret:create --name REVENUE_CAT_API_KEY --value "your_key"
